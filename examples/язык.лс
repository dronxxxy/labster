импорт лабстер.рендер.Размер
импорт лабстер.типы

число  = 0
список = (1, 2, 3)
строка = ф"просто строка %а"
мояРазметка = разметка
	== Моя разметка
	%число
всё

МойОбъект = объект
	_инициализировать: функ () то
		типы.проверить(объект
			моеПоле: типы.Число,
			второеПоле: типы.Строка.которая(схема.Строка.непустая),
		всё)

	отрисовать: функ (контекст) то
		контекст.отрисоватьТекст(я.моеПоле)
всё

мойОбъект = объект(МойОбъект)
	моеПоле: 100,
всё

мояФункция = функ (тест) то обернуть(объект
	моеПоле: 100,
всё, (МойОбъектРисует,))

